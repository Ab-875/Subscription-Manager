<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('../partials/navbar') %>
    <h1>Subscription Details</h1>

    <h2><%= foundSubscription.subName %></h2>
    <p>Category: <%= foundSubscription.subCategory  %></p>
    <p>Charge: <%= foundSubscription.price %></p>
    <p>Initial Subscription: <%= foundSubscription.startDate.toLocaleDateString("en-GB") %></p>
    <p>Rebill: <%= foundSubscription.endDate.toLocaleDateString("en-GB") %></p>
    <a href="/subscriptions/<%= foundSubscription._id %>/edit">edit</a>
    <form action="/subscriptions/<%= foundSubscription._id %>?_method=DELETE" method="post">
        <button type="submit">Delete</button>
    </form>
</body>
</html>